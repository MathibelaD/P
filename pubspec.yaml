name: my_first_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  # carousel_slider: ^4.2.1 # For image carousel
  # Maps_flutter: ^2.5.0 # For displaying the map
  url_launcher: ^6.2.1 # To open phone dialer/email app
  flutter_rating_bar: ^4.0.1
  supabase_flutter: ^2.0.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - .env
